import { FC } from 'react';

import '../../styles/minComponentsStyles/settings.scss';

const Settings: FC= () => {
    return (
        <div className="settings">
        </div>
    )
}

export default Settings;